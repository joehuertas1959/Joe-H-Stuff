DATABASE_URL="file:./mircs.db"
PORT=3001
FRONTEND_URL=http://localhost:5173

# Optional: provide an OpenAI API key to use real embeddings.
# If omitted, the server uses a built-in keyword-based vector fallback.
OPENAI_API_KEY=
